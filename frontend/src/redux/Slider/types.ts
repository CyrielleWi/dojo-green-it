export interface ImageUnsplashApi {
  id: string;
  alt_description: string;
  urls: {
    full: string;
  };
}

export interface ImageSlider {
  id: string;
  altDescription: string;
  url: string;
}
